const nombres = ['Marcos', 'Emilio', 'Juanca'];
const edades = [25, 30, 35];

const combinado = nombres.concat(edades);
console.log('esta es la combinacion:', combinado);

const cadena = combinado.join(', ');
console.log('esta es la cadena:', cadena);
